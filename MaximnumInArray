	#include <stdio.h>
using namespace std;

int main () {
	
  int a[] = {78, 5, 76,456, 8, 43, 23}, i, max;
  int length = sizeof(a) / sizeof(a[0]);
 
  max = a[0];
  
  for( i = 1; i < length; i++) {
    if(max < a[i]) {
    	max = a[i];
	}
  }
   printf("%d", max);
	return 0;
}
